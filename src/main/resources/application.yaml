spring:
  application:
    name: TaskManager
  datasource:
    url: jdbc:postgresql://localhost:5433/task_manager
    username: fara
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect